describe("Filter Methods", () => {
  it("Is filtering todo by first priority", () => {
    const result = priority1FilterMethod({ name: "asdasd", priority: 2 });

    expect(result).toBe(false);
  });

  it("is rendering todos", () => {
    expect(true).toBe(false);
  });

  it("is adding", () => {
    expect(true).toBe(false);
  });
});
